name: owlallsample
description: Owl All Samples

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3

  provider: ^1.6.1 #modal_bottom_sheet 예제를 위해 추가.

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
